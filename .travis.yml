sudo: required
jdk:
  - openjdk11
services:
  - docker
cache:
  directories:
    - .autoconf
    - ~/.m2
    - node_modules
before_script:
   - wget -qO- https://toolbelt.heroku.com/install.sh | sh
script:
  - ./build_and_push.sh
before_install:
  - docker pull openjdk:9
